# cloud-map-3d Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** RViz 里随狗行进逐步长出的 map 系累积 3D 散点地图(`/maze/cloud_map_3d`),同时全清旧 2D 地图 viz 三件套。

**Architecture:** 独立 TF 驱动累积节点(方案 1):纯核心类 `CloudMap3D`(3D 体素去重 + PointCloud2 导出)+ 薄节点 `cloud_map_accumulator`(订 `/lidar/points`,查 `map->cloud.frame_id` TF,latched 节流发布)。solver 只删不加;导航链(`scan_slice_projector → /scan → 感知/ICP`)零接触。

**Tech Stack:** ROS2 Jazzy, NumPy, sensor_msgs_py, tf2_ros, pytest。Spec: `docs/superpowers/specs/2026-07-22-cloud-map-3d-design.md`

---

## 硬约束(每个子代理都要遵守)

- 工作目录漂移频繁:**每个命令块都显式 `cd` 绝对路径**。工作空间 = `/home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate/ros2_ws_tugbot_nav_20260724`(下称 `$WS`;Task 0 创建)。
- 构建**必须** `colcon build --symlink-install`(plain build 会弄坏 maze_sim 数据路径);source 前先 `set +u`。只有 `python3`。
- git:`git add` 只用显式路径(严禁 `-A`,tmp_resources 有未跟踪大文件);commit 用 heredoc `git commit -F - <<'EOF'`,消息内**禁用反引号**,结尾 trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。
- **不许自行启动 GUI Gazebo**(Task 6 由用户明示后主会话执行);长跑 sim 批次必须在**主会话**起(子代理后台进程随子代理死——已付过 15.5h 学费)。
- 红线:`/gt/dynamic_pose` 真值通道不进任何新代码;新节点只消费 TF 与 `/lidar/points`。
- 不动区:`scan_slice_projector.py`、`slice_to_scan.py`、`online_scan_match_localizer.py`、`scan_match_localizer.py`、`maze_perception.py` 一行不改。

## 已核实的环境事实(不要重新考证)

- `/lidar/points`:PointCloud2,10Hz,1800×16 束,sensor 系,frame_id=`anymal_c/base/lidar_3d`;非回波是 inf(isfinite 滤掉)。
- TF 链:solver 发 `map->odom`(ICP);gz OdometryPublisher 发 `odom->base_link` 30Hz 全 3D;launch 静态 TF `base_link -> anymal_c/base/lidar_3d`(+0.35m)。
- **odom 系锚在 spawn 位姿**(spawn z=0.62)→ map 系里地面 ≈ **z −0.62**、狗背雷达 ≈ z −0.1~+0.3。故 z 裁剪取 `z_min=-1.0, z_max=3.0`(spec 写的 −0.2 是错的,Task 5 修 spec)。
- 2D viz 三件套消费者仅:`flood_fill_solver.py`、三个测试文件、`tugbot_nav.rviz`。
- 测试基线(20260723 克隆后):`python3 -m pytest src -q` → **10 failed / 494 passed / 3 xfailed**(Task 0 在源工作空间 20260723 上实测同数,克隆忠实;10 个失败=6 个 wall_follow 历史基线 + 4 个 legacy maze 资产契约,均先于本阶段存在。名单不许增减)。

---

### Task 0: 新工作空间 + 分支 + 基线

**Files:** 无源码改动;创建 `ros2_ws_tugbot_nav_20260724/`(整目录克隆)。

- [ ] **Step 1: 建分支(repo 根)**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate
git checkout -b cloud-map-3d
```

- [ ] **Step 2: 克隆工作空间(排除构建产物)**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate
rsync -a --exclude build --exclude install --exclude log --exclude __pycache__ \
  ros2_ws_tugbot_nav_20260723/ ros2_ws_tugbot_nav_20260724/
```

- [ ] **Step 3: 构建**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate/ros2_ws_tugbot_nav_20260724
set +u; source /opt/ros/jazzy/setup.bash; colcon build --symlink-install
```

Expected: 全部包 build 成功,0 abort。

- [ ] **Step 4: 基线测试**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate/ros2_ws_tugbot_nav_20260724
set +u; source /opt/ros/jazzy/setup.bash; source install/setup.bash
python3 -m pytest src -q 2>&1 | tail -3
```

Expected: `10 failed, 494 passed, 3 xfailed`(与 20260723 源工作空间实测完全一致;原计划写的 7/492 是过期数字,Task 0 实测修正)。

- [ ] **Step 5: Commit**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate
git add ros2_ws_tugbot_nav_20260724
git commit -F - <<'EOF'
chore: clone workspace 20260724 from 20260723 for cloud-map-3d

Baseline: build clean, pytest 10 failed / 494 passed / 3 xfailed --
measured IDENTICAL on the source workspace 20260723 (6 wall_follow
historical + 4 legacy maze-asset contract failures, all pre-existing).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

---

### Task 1: `CloudMap3D` 纯核心(TDD)

**Files:**
- Create: `$WS/src/tugbot_maze/tugbot_maze/cloud_map_3d.py`
- Test: `$WS/src/tugbot_maze/test/test_cloud_map_3d.py`

- [ ] **Step 1: 写失败测试(完整文件)**

```python
"""CloudMap3D: map-frame 3D voxel accumulation. Pure NumPy + msg construction,
mirrors test_scatter_cloud.py conventions (no rclpy.init needed)."""
import math
import struct

import numpy as np

from tugbot_maze.cloud_map_3d import CloudMap3D, should_publish, transform_to_matrix


def _decode_xyz(msg):
    data = bytes(msg.data)
    out = []
    for i in range(msg.width):
        out.append(struct.unpack_from('<fff', data, i * msg.point_step))
    return out


def _quat_yaw(t):
    return (0.0, 0.0, math.sin(t / 2.0), math.cos(t / 2.0))


def _quat_pitch(t):
    return (0.0, math.sin(t / 2.0), 0.0, math.cos(t / 2.0))


def test_transform_identity_translation():
    T = transform_to_matrix(1.0, 2.0, 3.0, 0.0, 0.0, 0.0, 1.0)
    p = T @ np.array([0.5, 0.0, 0.0, 1.0])
    assert np.allclose(p[:3], (1.5, 2.0, 3.0))


def test_transform_yaw_90():
    qx, qy, qz, qw = _quat_yaw(math.pi / 2.0)
    T = transform_to_matrix(0.0, 0.0, 0.0, qx, qy, qz, qw)
    p = T @ np.array([1.0, 0.0, 0.0, 1.0])
    assert np.allclose(p[:3], (0.0, 1.0, 0.0), atol=1e-9)


def test_transform_pitch_moves_forward_point_down():
    # +pitch (about +y) rotates a forward point toward -z: trot 俯仰补偿的关键路径。
    qx, qy, qz, qw = _quat_pitch(math.radians(10.0))
    T = transform_to_matrix(0.0, 0.0, 0.0, qx, qy, qz, qw)
    p = T @ np.array([1.0, 0.0, 0.0, 1.0])
    assert np.allclose(p[:3], (math.cos(math.radians(10)), 0.0, -math.sin(math.radians(10))), atol=1e-9)


def test_add_cloud_dedup_same_cloud_twice():
    m = CloudMap3D(voxel_m=0.05)
    pts = np.array([[1.0, 0.0, 0.0], [2.0, 1.0, 0.5]])
    T = np.eye(4)
    a1 = m.add_cloud(pts, T)
    a2 = m.add_cloud(pts, T)
    assert a1 == 2 and a2 == 0 and len(m) == 2


def test_add_cloud_range_filter():
    m = CloudMap3D(voxel_m=0.05, usable_range_m=8.0)
    pts = np.array([[7.0, 0.0, 0.0], [9.0, 0.0, 0.0]])   # sensor-frame norm 7 / 9
    assert m.add_cloud(pts, np.eye(4)) == 1


def test_add_cloud_drops_nonfinite():
    m = CloudMap3D(voxel_m=0.05)
    pts = np.array([[np.inf, 0.0, 0.0], [np.nan, 1.0, 0.0], [1.0, 1.0, 0.0]])
    assert m.add_cloud(pts, np.eye(4)) == 1 and len(m) == 1


def test_add_cloud_empty_input():
    m = CloudMap3D()
    assert m.add_cloud(np.zeros((0, 3)), np.eye(4)) == 0 and len(m) == 0


def test_add_cloud_z_clip_in_map_frame():
    # 地面在 map 系 z ~ -0.62(odom 锚在 spawn z=0.62)必须保留;z<-1.0 与 z>3.0 裁掉。
    m = CloudMap3D(voxel_m=0.05, z_min=-1.0, z_max=3.0)
    pts = np.array([[1.0, 0.0, -0.62], [1.0, 1.0, -1.5], [1.0, 2.0, 3.5]])
    assert m.add_cloud(pts, np.eye(4)) == 1


def test_to_pointcloud2_roundtrip():
    m = CloudMap3D(voxel_m=0.05)
    pts = np.array([[1.02, -0.51, 0.26], [3.99, 2.0, -0.6]])
    m.add_cloud(pts, np.eye(4))
    out = _decode_xyz(m.to_pointcloud2(frame_id='map'))
    assert len(out) == 2
    for orig in pts:
        assert min(max(abs(a - b) for a, b in zip(orig, got)) for got in out) <= 0.05 / 2 + 1e-6


def test_to_pointcloud2_empty():
    msg = CloudMap3D().to_pointcloud2()
    assert msg.width == 0 and msg.data == b'' and msg.header.frame_id == 'map'


def test_should_publish_first_frame_always():
    assert should_publish(None, 100.0, 0, 1.0) is True


def test_should_publish_growth_and_period():
    assert should_publish(100.0, 101.5, 5, 1.0) is True     # 有增长且过周期
    assert should_publish(100.0, 101.5, 0, 1.0) is False    # 无增长
    assert should_publish(100.0, 100.4, 5, 1.0) is False    # 增长但未到周期
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate/ros2_ws_tugbot_nav_20260724
set +u; source /opt/ros/jazzy/setup.bash; source install/setup.bash
python3 -m pytest src/tugbot_maze/test/test_cloud_map_3d.py -q 2>&1 | tail -3
```

Expected: FAIL/ERROR — `ModuleNotFoundError: tugbot_maze.cloud_map_3d`。

- [ ] **Step 3: 实现(完整文件)**

```python
"""Map-frame 3D voxel accumulation for the cloud-map-3d viz: filter a sensor-frame
lidar cloud (finite, range<=usable), transform by a 4x4 map<-sensor matrix, clip
to a sane map-frame z band (the odom frame is anchored at the spawn pose z=0.62,
so the GROUND sits near map z=-0.62 -- z_min must stay below it), and dedup into
an integer voxel set exported as an xyz-float32 PointCloud2. Pure NumPy + msg
construction -- no ROS node state, no rclpy.init needed (mirrors scatter_cloud's
old contract, generalized to 3D)."""
from __future__ import annotations

import numpy as np
from sensor_msgs.msg import PointCloud2, PointField


def transform_to_matrix(tx: float, ty: float, tz: float,
                        qx: float, qy: float, qz: float, qw: float) -> np.ndarray:
    """4x4 homogeneous map<-sensor matrix from a TF translation + quaternion."""
    xx, yy, zz = qx * qx, qy * qy, qz * qz
    xy, xz, yz = qx * qy, qx * qz, qy * qz
    wx, wy, wz = qw * qx, qw * qy, qw * qz
    T = np.eye(4)
    T[:3, :3] = [[1 - 2 * (yy + zz), 2 * (xy - wz), 2 * (xz + wy)],
                 [2 * (xy + wz), 1 - 2 * (xx + zz), 2 * (yz - wx)],
                 [2 * (xz - wy), 2 * (yz + wx), 1 - 2 * (xx + yy)]]
    T[:3, 3] = (tx, ty, tz)
    return T


def should_publish(last_pub_s, now_s: float, added: int, period_s: float) -> bool:
    """First frame always publishes; afterwards only when the map grew AND the
    throttle period elapsed (the old scatter-cloud cadence contract: a growth
    inside the throttle window is published by the NEXT growth's full-set
    publish, so nothing is lost, cadence just lags)."""
    if last_pub_s is None:
        return True
    return added > 0 and (now_s - last_pub_s) >= period_s


class CloudMap3D:
    def __init__(self, voxel_m: float = 0.05, usable_range_m: float = 8.0,
                 z_min: float = -1.0, z_max: float = 3.0) -> None:
        self.voxel_m = float(voxel_m)
        self.usable_range_m = float(usable_range_m)
        self.z_min = float(z_min)
        self.z_max = float(z_max)
        self._voxels: set[tuple[int, int, int]] = set()

    def add_cloud(self, points_xyz, T_map_sensor) -> int:
        """Merge one sensor-frame cloud into the accumulated map-frame voxel set.
        Returns the number of NEW voxels added."""
        p = np.asarray(points_xyz, dtype=float).reshape(-1, 3)
        if p.size == 0:
            return 0
        p = p[np.isfinite(p).all(axis=1)]
        if p.size == 0:
            return 0
        rng = np.linalg.norm(p, axis=1)
        p = p[(rng > 0.0) & (rng <= self.usable_range_m)]
        if p.size == 0:
            return 0
        T = np.asarray(T_map_sensor, dtype=float)
        m = p @ T[:3, :3].T + T[:3, 3]
        m = m[(m[:, 2] >= self.z_min) & (m[:, 2] <= self.z_max)]
        if m.size == 0:
            return 0
        keys = np.round(m / self.voxel_m).astype(np.int64)
        before = len(self._voxels)
        self._voxels.update(map(tuple, keys.tolist()))
        return len(self._voxels) - before

    def to_pointcloud2(self, frame_id: str = 'map', stamp=None) -> PointCloud2:
        """Export the voxel set as xyz-float32 (keys * voxel_m, deterministic order)."""
        msg = PointCloud2()
        msg.header.frame_id = frame_id
        if stamp is not None:
            msg.header.stamp = stamp
        msg.height = 1
        msg.width = len(self._voxels)
        msg.fields = [
            PointField(name='x', offset=0, datatype=PointField.FLOAT32, count=1),
            PointField(name='y', offset=4, datatype=PointField.FLOAT32, count=1),
            PointField(name='z', offset=8, datatype=PointField.FLOAT32, count=1),
        ]
        msg.is_bigendian = False
        msg.point_step = 12
        msg.row_step = 12 * msg.width
        msg.is_dense = True
        if self._voxels:
            keys = np.array(sorted(self._voxels), dtype=np.float64)
            msg.data = (keys * self.voxel_m).astype('<f4').tobytes()
        else:
            msg.data = b''
        return msg

    def __len__(self) -> int:
        return len(self._voxels)
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate/ros2_ws_tugbot_nav_20260724
set +u; source /opt/ros/jazzy/setup.bash; source install/setup.bash
python3 -m pytest src/tugbot_maze/test/test_cloud_map_3d.py -q 2>&1 | tail -3
```

Expected: `12 passed`。

- [ ] **Step 5: Commit**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate
git add ros2_ws_tugbot_nav_20260724/src/tugbot_maze/tugbot_maze/cloud_map_3d.py \
        ros2_ws_tugbot_nav_20260724/src/tugbot_maze/test/test_cloud_map_3d.py
git commit -F - <<'EOF'
feat: CloudMap3D pure core (3D voxel accumulation -> PointCloud2)

Sensor-frame filter (finite, range<=8m) -> 4x4 map<-sensor transform (full
roll/pitch support, quaternion-driven) -> map-frame z clip [-1.0, 3.0] (odom
anchored at spawn z=0.62 puts the ground near map z=-0.62) -> integer voxel
dedup at 0.05m -> deterministic xyz-float32 export. Plus should_publish
(first-frame / growth+period throttle contract). 12 tests.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

---

### Task 2: `cloud_map_accumulator` 节点 + 接线

**Files:**
- Create: `$WS/src/tugbot_maze/tugbot_maze/cloud_map_accumulator.py`
- Modify: `$WS/src/tugbot_maze/setup.py`(console_scripts,现第 27-36 行)
- Modify: `$WS/src/tugbot_bringup/launch/tugbot_maze_explore.launch.py`(节点定义区 + 第 367 行 TimerAction)
- Modify: `$WS/tools/run_flood_fill_maze.sh`(pkill 名单第 43-45 行、tail grep 第 78 行)

- [ ] **Step 1: 节点实现(完整文件)**

```python
#!/usr/bin/env python3
"""Accumulated 3D scatter map for RViz: subscribes the 16-beam lidar cloud
(/lidar/points, sensor frame), transforms each frame to the map frame via one
TF lookup (map->odom = the solver's ICP-corrected pose; odom->base_link = gz's
full-3D odometry incl. walking roll/pitch; base_link->anymal_c/base/lidar_3d =
the launch static mount TF), voxel-dedups into CloudMap3D and republishes the
accumulation (latched) on /maze/cloud_map_3d. Pure viz in its own process --
consumes only TF + the cloud, never touches navigation state. Frames without a
resolvable TF are dropped silently (normal until the solver seeds map->odom).
Launch-gated to flood_fill + online_slam."""
import rclpy
from rclpy.node import Node
from rclpy.qos import QoSProfile, DurabilityPolicy
from rclpy.time import Time
from sensor_msgs.msg import PointCloud2
from sensor_msgs_py import point_cloud2
import tf2_ros

from tugbot_maze.cloud_map_3d import CloudMap3D, should_publish, transform_to_matrix


class CloudMapAccumulator(Node):
    def __init__(self):
        super().__init__('cloud_map_accumulator')
        self.map_frame = self.declare_parameter('map_frame', 'map').value
        self.publish_period_s = float(self.declare_parameter('publish_period_s', 1.0).value)
        self._map = CloudMap3D()
        self._last_pub_s = None
        self.tf_buffer = tf2_ros.Buffer()
        self.tf_listener = tf2_ros.TransformListener(self.tf_buffer, self)
        qos = QoSProfile(depth=1)
        qos.durability = DurabilityPolicy.TRANSIENT_LOCAL   # latch for late RViz joins
        self.pub = self.create_publisher(PointCloud2, '/maze/cloud_map_3d', qos)
        self.create_subscription(PointCloud2, '/lidar/points', self._cloud_cb, 5)
        self.get_logger().info('cloud_map_accumulator started.')

    def _cloud_cb(self, cloud):
        try:
            try:
                t = self.tf_buffer.lookup_transform(          # latest available
                    self.map_frame, cloud.header.frame_id, Time())
            except tf2_ros.TransformException:
                return
            tr, q = t.transform.translation, t.transform.rotation
            T = transform_to_matrix(tr.x, tr.y, tr.z, q.x, q.y, q.z, q.w)
            pts = point_cloud2.read_points_numpy(
                cloud, field_names=('x', 'y', 'z'), skip_nans=False)
            added = self._map.add_cloud(pts, T)
            now = self.get_clock().now()
            now_s = now.nanoseconds / 1e9
            if should_publish(self._last_pub_s, now_s, added, self.publish_period_s):
                self.pub.publish(self._map.to_pointcloud2(
                    frame_id=self.map_frame, stamp=now.to_msg()))
                self._last_pub_s = now_s
                self.get_logger().info('CLOUDMAP voxels=%d' % len(self._map))
        except Exception as e:              # viz must never crash or spam
            self.get_logger().error('cloud frame dropped: %r' % e,
                                    throttle_duration_sec=5.0)


def main(args=None):
    rclpy.init(args=args)
    node = CloudMapAccumulator()
    try:
        rclpy.spin(node)
    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == '__main__':
    main()
```

- [ ] **Step 2: setup.py 注册入口**

在 `console_scripts` 列表 `'scan_slice_projector = tugbot_maze.scan_slice_projector:main',` 之后加一行:

```python
            'cloud_map_accumulator = tugbot_maze.cloud_map_accumulator:main',
```

- [ ] **Step 3: launch 接线(tugbot_maze_explore.launch.py)**

在 `locomotion_controller = Node(...)` 定义之前插入(与既有 Node 定义同缩进层级):

```python
    cloud_map_accumulator = Node(
        package='tugbot_maze', executable='cloud_map_accumulator', name='cloud_map_accumulator',
        output='screen',
        parameters=[{'use_sim_time': ParameterValue(LaunchConfiguration('use_sim_time'), value_type=bool)}],
        condition=IfCondition(PythonExpression([
            "'", explorer_type, "' == 'flood_fill' and '",
            LaunchConfiguration('pose_source'), "' == 'online_slam'"])),
    )
```

并把第 367 行 TimerAction 的 actions 列表尾部加上它:

```python
        TimerAction(period=13.0, actions=[maze_dfs_explorer, frontier_explorer, maze_solver_node, wall_follow_solver_node, flood_fill_solver_node, cloud_map_accumulator]),
```

- [ ] **Step 4: run 脚本(tools/run_flood_fill_maze.sh)**

`kill_all_sim` 的模式名单里 `flood_fill_solver locomotion_controller scan_slice_projector \` 一行改为:

```bash
               flood_fill_solver locomotion_controller scan_slice_projector cloud_map_accumulator \
```

第 78 行 tail grep 的模式串 `"EXIT_REACHED|HOP_BACKUP|..."` 里追加 `|CLOUDMAP`。

- [ ] **Step 5: 构建 + 冒烟**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate/ros2_ws_tugbot_nav_20260724
set +u; source /opt/ros/jazzy/setup.bash; colcon build --symlink-install 2>&1 | tail -3
source install/setup.bash
python3 -c "from tugbot_maze.cloud_map_accumulator import CloudMapAccumulator; print('import ok')"
ros2 pkg executables tugbot_maze | grep cloud_map_accumulator
```

Expected: build 成功;`import ok`;`tugbot_maze cloud_map_accumulator`。

- [ ] **Step 6: Commit**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate
git add ros2_ws_tugbot_nav_20260724/src/tugbot_maze/tugbot_maze/cloud_map_accumulator.py \
        ros2_ws_tugbot_nav_20260724/src/tugbot_maze/setup.py \
        ros2_ws_tugbot_nav_20260724/src/tugbot_bringup/launch/tugbot_maze_explore.launch.py \
        ros2_ws_tugbot_nav_20260724/tools/run_flood_fill_maze.sh
git commit -F - <<'EOF'
feat: cloud_map_accumulator node publishing /maze/cloud_map_3d

TF-driven standalone viz process: one lookup map->cloud frame per frame
(ICP xy/yaw composed with full-3D odom roll/pitch/z and the static lidar
mount), CloudMap3D voxel accumulation, latched throttled publish, CLOUDMAP
voxel-count log line per publish (the headless acceptance probe). Gated to
flood_fill + online_slam in the explore launch; kill list + tail grep wired
in the run script. Solver untouched.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

---

### Task 3: 全清 2D viz 三件套

**Files:**
- Modify: `$WS/src/tugbot_maze/tugbot_maze/flood_fill_solver.py`(imports 区、`__init__` 116-130 行区、三个方法 279-345 行区、`_control_tick` 三个调用点 ~410 行区)
- Delete: `$WS/src/tugbot_maze/tugbot_maze/{scatter_cloud,radar_occupancy,flood_fill_viz}.py`
- Delete: `$WS/src/tugbot_maze/test/{test_scatter_cloud,test_radar_occupancy,test_flood_fill_viz}.py`
- Modify: `$WS/src/tugbot_bringup/rviz/tugbot_nav.rviz`(删 SelfBuiltMap/SelfBuiltWalls/ScatterCloud 三块,加 CloudMap3D)

- [ ] **Step 1: solver 摘除(逐处)**

1. 删三行模块 import:`from tugbot_maze.flood_fill_viz import self_built_wall_markerarray`、`from tugbot_maze.scatter_cloud import ScatterCloud`、`from tugbot_maze.radar_occupancy import RadarOccupancyGrid`。
2. 消息 import 收缩:`from sensor_msgs.msg import LaserScan, PointCloud2` → `from sensor_msgs.msg import LaserScan`;整行删 `from nav_msgs.msg import OccupancyGrid`、`from visualization_msgs.msg import MarkerArray`、`from rclpy.qos import QoSProfile, DurabilityPolicy`(删前 grep 确认这些名字在文件里再无别的使用点)。
3. `online_scan_match_localizer` import 里去掉 `confirmed_wall_segments`(唯一使用点在被删的 `_publish_self_built_walls`;`local_reference_cells` 保留——删前 grep 确认)。
4. `__init__` 里删掉整块(从 `_walls_qos = QoSProfile(depth=1)` 到 `self._walls_key = None`,含 walls_pub/map_pub/scatter_pub、scatter_period_s、`_scatter`/`_scatter_last_pub`、`_radar`/`_radar_last_pub`)。**保留**其上的 tf_buffer/listener/broadcaster 与其下的 `self.phase = 'startup'`。**保留** `self._perimeter_segments`(第 99 行,localizer 在用)。
5. 整方法删除:`_publish_self_built_walls`、`_publish_scatter_cloud`、`_publish_radar_map`。
6. `_control_tick` 里删三行调用:`self._publish_self_built_walls()`、`self._publish_scatter_cloud(now)`、`self._publish_radar_map(now)`。

- [ ] **Step 2: 删文件**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate
git rm ros2_ws_tugbot_nav_20260724/src/tugbot_maze/tugbot_maze/scatter_cloud.py \
       ros2_ws_tugbot_nav_20260724/src/tugbot_maze/tugbot_maze/radar_occupancy.py \
       ros2_ws_tugbot_nav_20260724/src/tugbot_maze/tugbot_maze/flood_fill_viz.py \
       ros2_ws_tugbot_nav_20260724/src/tugbot_maze/test/test_scatter_cloud.py \
       ros2_ws_tugbot_nav_20260724/src/tugbot_maze/test/test_radar_occupancy.py \
       ros2_ws_tugbot_nav_20260724/src/tugbot_maze/test/test_flood_fill_viz.py
```

- [ ] **Step 3: RViz 配置**

`tugbot_nav.rviz` 删三个显示块(YAML 列表项,各以 `- Alpha:`/`- Class:` 开头):`Name: SelfBuiltMap` 的 Map 块、`Name: SelfBuiltWalls` 的 MarkerArray 块、`Name: ScatterCloud` 的 PointCloud2 块。**保留** Path、Lidar3D、LaserScan、FrontCamera。在 Lidar3D 块之后同缩进插入:

```yaml
    - Alpha: 1
      Autocompute Intensity Bounds: true
      Autocompute Value Bounds:
        Max Value: 10
        Min Value: -10
        Value: true
      Axis: Z
      Channel Name: intensity
      Class: rviz_default_plugins/PointCloud2
      Color: 255; 255; 255
      Color Transformer: AxisColor
      Decay Time: 0
      Enabled: true
      Invert Rainbow: false
      Max Color: 255; 255; 255
      Min Color: 0; 0; 0
      Name: CloudMap3D
      Position Transformer: XYZ
      Selectable: false
      Size (Pixels): 3
      Size (m): 0.03
      Style: Flat Squares
      Topic:
        Depth: 1
        Durability Policy: Transient Local
        Filter size: 10
        History Policy: Keep Last
        Reliability Policy: Reliable
        Value: /maze/cloud_map_3d
      Use Fixed Frame: true
      Use rainbow: true
      Value: true
```

- [ ] **Step 4: 残留扫描 + 构建 + 全套件**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate/ros2_ws_tugbot_nav_20260724
grep -rn "scatter_cloud\|radar_occupancy\|flood_fill_viz\|self_built_map\|self_built_walls" src/ tools/ --include="*.py" --include="*.rviz" --include="*.sh" | grep -v __pycache__
set +u; source /opt/ros/jazzy/setup.bash; colcon build --symlink-install 2>&1 | tail -3
source install/setup.bash
python3 -m pytest src -q 2>&1 | tail -3
```

Expected: grep **零输出**;build 成功;`10 failed, 489 passed, 3 xfailed`(494 − 18 删 + 13 新;10 个失败名单与基线逐名一致。Task 1 质量审查后加了第 13 个帧契约测试,且导出改 lexsort——sorted() 在 40 万体素时 ~760ms 会卡回调线程)。

- [ ] **Step 5: Commit**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate
git add ros2_ws_tugbot_nav_20260724/src/tugbot_maze/tugbot_maze/flood_fill_solver.py \
        ros2_ws_tugbot_nav_20260724/src/tugbot_bringup/rviz/tugbot_nav.rviz
git commit -F - <<'EOF'
feat!: remove the legacy 2D map viz chain (scatter/self_built_map/walls)

User-decided full removal: the three viz products, their modules and tests,
the solver wiring (imports, publishers, three publish methods, tick calls)
and the three RViz displays -- replaced by the CloudMap3D display on
/maze/cloud_map_3d. Solver net-shrinks; localizer/perception untouched
(_perimeter_segments retained for the ICP reference). Suite: 10 failed /
489 passed / 3 xfailed (baseline names unchanged; -18 removed, +13 new).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

(git rm 的六个文件已在暂存区,随本 commit 一并落。)

---

### Task 4: spec 修正(z 裁剪 + 断言机制)

**Files:**
- Modify: `docs/superpowers/specs/2026-07-22-cloud-map-3d-design.md`

- [ ] **Step 1: 修两处**

1. `z_min=-0.2, z_max=3.0` → `z_min=-1.0, z_max=3.0`,并在 4.1 节末尾追加一句:「z 基准修正:odom 系锚在 spawn 位姿(z=0.62),map 系地面 ≈ −0.62,故 z_min 取 −1.0(−0.2 会裁掉地面)。」
2. 验收第 2 条"跑内录制末条 latched 消息断言 width" → "grep launch.log 末条 CLOUDMAP voxels=N 日志断言 N > 50,000(节点每次发布记一行,机制等价、无需配对 DDS 域的录制进程)"。

- [ ] **Step 2: Commit**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate
git add docs/superpowers/specs/2026-07-22-cloud-map-3d-design.md
git commit -F - <<'EOF'
docs: spec corrections for cloud-map-3d (z clip band, assertion mechanism)

z_min -1.0 (odom anchored at spawn z=0.62 puts the ground near map z=-0.62;
the spec's -0.2 would have clipped the floor) and the headless voxel
assertion reads the CLOUDMAP log line instead of recording the latched
message (equivalent evidence, no DDS-domain-paired recorder process).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
```

---

### Task 5: headless×2 统计验收(主会话执行,不派子代理)

**Files:** 无改动。⚠️ 长跑必须由主会话直接起(子代理后台进程随子代理终止)。

- [ ] **Step 1: 跑两次 headless(串行,后台起,POSE_DIAG 默认关)**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate/ros2_ws_tugbot_nav_20260724
bash tools/run_flood_fill_maze.sh 1500 true false online_slam
```

(第一跑收尾后同命令再跑第二次;每跑结束记下 `ARTIFACT_DIR`。)

- [ ] **Step 2: 每跑三道门**

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate/ros2_ws_tugbot_nav_20260724
ART=<ARTIFACT_DIR>
cat "$ART/result.txt"
grep -aoE "CLOUDMAP voxels=[0-9]+" "$ART/launch.log" | tail -1
set +u; source /opt/ros/jazzy/setup.bash; source install/setup.bash
python3 tools/replay_collision_oracle.py "$ART"
```

Expected(每跑):`EXIT_REACHED`;末条 CLOUDMAP `voxels=N` 且 **N > 50000**;oracle 碰撞率 **0.000%**。另查 `grep -ac "ERROR" "$ART/launch.log"` 无新增 ERROR 类别(cloud frame dropped 若成流须取证)。

- [ ] **Step 3: 任一门失败 → 停手取证**

先取证(launch.log、CLOUDMAP 序列、TF 可用性时间线),不改代码不重跑刷结果;把证据带回主会话裁决。两跑全过 → 记录两个 ARTIFACT_DIR 与体素终值,进 Task 6。

---

### Task 6: GUI 验收(用户门,禁止自启)

- [ ] **Step 1: 等用户明示后**(主会话)起 GUI:

```bash
cd /home/hyh/Desktop/agent_playground/playground_claude_code/ros2_gazebo_tugbot_navigate/ros2_ws_tugbot_nav_20260724
bash tools/run_flood_fill_maze.sh 1500 false true online_slam
```

- [ ] **Step 2: 用户看三点**:3D 图随狗行进逐步长出且不动的部分稳定(无重影/涂抹);与 Lidar3D 实时帧贴合;高度染色墙/地可辨;旧三件显示不在。通过口令后走 finishing 流程(全分支终审 → merge --no-ff → 套件复验 → push → 删分支 → 记忆归档)。

---

## Self-Review 备注

- Spec 覆盖:4.1→Task 1;4.2→Task 2 Step 1;4.3→Task 2 Steps 3-4;4.4→Task 3 Step 3;4.5→Task 3;§5 红线→节点零真值、launch 门控;§6 验收→Tasks 1/5/6;§7 风险→节点静默丢帧+latched。z_min 与断言机制两处 spec 偏差由 Task 4 显式修 spec(带理由),非静默偏离。
- 类型一致:`transform_to_matrix(tx,ty,tz,qx,qy,qz,qw)`、`should_publish(last_pub_s,now_s,added,period_s)`、`add_cloud(points_xyz,T_map_sensor)` 在 Task 1 定义、Task 2 同名使用;`CLOUDMAP voxels=` 日志样式在 Task 2/4/5 一致。
- 数字自洽:494 − 18(7+2+9)+ 13 = 489。(Task 0 实测把过期基线 7/492 修正为 10/494,两工作空间同数,克隆忠实性已证;Task 1 质量审查加第 13 测 + lexsort 导出。)
